Snapshot: 2026-09-10
Section: Capabilities / “What we build”
Live file: components/sections/Capabilities.tsx
Mounted in: app/page.tsx inside the z-[4] curtain, between WhatWeStandOn and WhatWeDontDo

This folder is a frozen copy of the tabbed category UI (Product / Engineering / Applied AI / Platform / Partnership) with the auto-cycling list and right-hand detail card.

To restore this UI on Capabilities:
  Copy Capabilities.tsx from this folder over components/sections/Capabilities.tsx
  Keep the page.tsx import: import { Capabilities } from "@/components/sections/Capabilities";
  Keep <Capabilities /> where it is (or mount it wherever you want the same UI)

To reuse this UI on another section:
  Copy Capabilities.tsx, rename the function/id/copy, and import it from page.tsx

Self-contained: one file. Uses framer-motion, clsx, and existing type-* / color tokens. No section-specific CSS.
